<div class="bg-info text-center top-info">
  <p>
    <h4>All Tasks</h4><br>
    You can create a new task and associate it with a single date, or create a recurring task that will appear each week on a specified day.
  </p>
</div><table class="table">
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @tasks.each do |t| %>
    <tr>
      <td><%= t.title %></td>
      <td><%= t.description %></td>
      <td><%= link_to "Show", task_path(t), class: "btn btn-default" %></td>
      <td><%= link_to "Edit", edit_task_path(t), class: "btn btn-default" %></td>
      <td><%= link_to "Delete", t, method: :delete, data: {confirm: "Are you sure?" }, class: "btn btn-danger" %>
    </tr>
  <% end %>
</table>

<%= link_to "New Task", new_task_path, class: "btn btn-primary" %>
